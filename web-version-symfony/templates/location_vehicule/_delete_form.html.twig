
<form method="post" action="{{ path('app_location_vehicule_delete', {'id_loc_vehicule': location_vehicule.Idlocvehicule}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ location_vehicule.Idlocvehicule) }}">
    <button class="btn" style="display: block; margin: auto;background-color: #047ffc; color: white;padding: 10px 25px;text-transform: uppercase;font-size: 14px;">Delete</button>
</form>
